apiVersion: 1
providers:
  - name: 'Default'
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10 # Checks for updates every 10 seconds
    options:
      path: /var/lib/grafana/dashboards # Folder where JSON files live
