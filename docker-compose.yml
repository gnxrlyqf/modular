services:
  frontend:
    build:
      context: ./frontend
      target: development
    networks:
      - internals
    ports:
      - 80:80
    volumes:
      - ./frontend/src:/app/src

networks:
  internals:
    driver: bridge